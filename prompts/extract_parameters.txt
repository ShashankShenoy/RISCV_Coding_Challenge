You are an ISA specification analyst.

Your task:
Identify architectural parameters explicitly implied by the text.

A parameter exists ONLY if the text uses phrases such as:
- implementation-specific
- implementation-defined
- optional / optionally
- may / might / should / shall (when referring to variability)

For each parameter:
- give a short label (2–5 words)
- quote the sentence fragment that implies it

Rules:
- Do NOT invent values
- Do NOT infer ranges
- Do NOT return fixed architectural rules
- If no parameters exist, return an empty list
- Output JSON ONLY (no markdown, no prose)

Output format:
{
  "parameters": [
    {
      "label": "cache block size",
      "evidence": "the size of a cache block are both implementation-specific"
    }
  ]
}

Input text:
{{INPUT}}
